# linear-cell

The functions inside of Tools.py which print information to the screen and display information directly to the user use state numbers in base 10, whereas the functions that deal with the actual calculation use binary.

A massive bitarray may be used to show if a position is an N-position (0) or a P-position (1). The default is 0, this is okay since the main priority is searching for P-positions